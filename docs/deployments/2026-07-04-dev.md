# Innernote Dev Deployment Prep

Date: 2026-07-04
Owner: agent
Status: draft
Related skill: deployment
Product version: pending
Source context: server.mjs, src/journal-files.js, src/journal-db.js, src/journal-store.js, AnyHost CLI setup output

## Change

Prepared Innernote for AnyHost dev deployment with a Dockerfile, health endpoint, and Postgres-backed journal storage when `DATABASE_URL` is present. Local file storage remains the fallback when `DATABASE_URL` is absent.

Added a migration command for moving local journal JSON and review summaries into Postgres:

```sh
npm run migrate:journals
```

## Target

Target environment: AnyHost `dev`.

Repository: `reizz25/innernote`.

Current blocker: AnyHost CLI still reports that `reizz25/innernote` is not imported:

```text
repository is not imported in AnyHost; import it first: https://contextnotcontrol.ai/github/setup/?repo=reizz25%2Finnernote
```

## Commands

Setup and inspection commands run:

```sh
anyhost version
anyhost setup-agent --skip-link
anyhost login
anyhost setup-agent
```

Verification commands run:

```sh
node --test test/*.test.mjs
PORT=4181 node server.mjs
node -e "fetch('http://127.0.0.1:4181/api/health')..."
node -e "fetch('http://127.0.0.1:4181/api/journals')..."
```

## Product State

Local smoke test used file storage because no `DATABASE_URL` was configured locally.

Observed local data:

- Journal API returned 6 entries from `/Users/zz/Desktop/InnerNotes/journals`.
- Review API returned 1 summary from `/Users/zz/Desktop/InnerNotes/reviews`.

Expected deployed behavior:

- AnyHost ready Postgres resource injects `DATABASE_URL`.
- Server starts in `postgres` storage mode.
- `/api/health` returns `{ "ok": true, "storage": "postgres" }`.
- Migration script imports local JSON entries and review summaries into Postgres.

## Verification

Automated tests passed:

```text
tests 76
pass 76
fail 0
```

Local smoke test passed:

```text
GET /api/health -> 200 {"ok":true,"storage":"file"}
GET /api/journals -> 200 entries=6 summaries=1
```

## Risks

- AnyHost project is not bound yet, so managed resource context and dev deployment have not been verified.
- Postgres migration has been unit-tested against the storage contract but has not run against a live AnyHost Postgres resource.
- Image assets still use local filesystem storage. Notes text and review summaries are database-ready; pasted image persistence should move to object storage before relying on it in production.

## Follow-Up

1. Complete AnyHost repository import for `reizz25/innernote`.
2. Rerun `anyhost setup-agent` and `anyhost context`.
3. Confirm a ready dev Postgres resource exists.
4. Commit and push deployable changes.
5. Run `anyhost deploy -e dev`.
6. Run `npm run migrate:journals` with a dev database access profile or an equivalent authorized `DATABASE_URL`.
