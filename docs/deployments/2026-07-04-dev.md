# Innernote Dev Deployment Prep

Date: 2026-07-04
Owner: agent
Status: implemented
Related skill: deployment
Product version: pending
Source context: server.mjs, src/journal-files.js, src/journal-db.js, src/journal-store.js, AnyHost CLI setup output

## Change

Prepared Innernote for AnyHost dev deployment with a Dockerfile, health endpoint, and Postgres-backed journal storage when `DATABASE_URL` is present. Local file storage remains the fallback when `DATABASE_URL` is absent.

Added a migration command for moving local journal JSON and review summaries into Postgres:

```sh
npm run migrate:journals
```

## Target

Target environment: AnyHost `dev`.

Repository: `reizz25/innernote`.

Current state: repository is imported and linked to AnyHost. Dev deployment succeeded at `https://innernote-dev.anyhostcloud.com`.

## Commands

Setup and inspection commands run:

```sh
anyhost version
anyhost setup-agent --skip-link
anyhost login
anyhost link --repo-url https://github.com/reizz25/innernote
anyhost setup-agent
anyhost context
anyhost db list -e dev
anyhost deploy -e dev
anyhost status
node -e "fetch('https://innernote-dev.anyhostcloud.com/api/health')..."
```

Verification commands run:

```sh
node --test test/*.test.mjs
PORT=4181 node server.mjs
node -e "fetch('http://127.0.0.1:4181/api/health')..."
node -e "fetch('http://127.0.0.1:4181/api/journals')..."
```

## Product State

Local smoke test used file storage because no `DATABASE_URL` was configured locally.

Observed local data:

- Journal API returned 6 entries from `/Users/zz/Desktop/InnerNotes/journals`.
- Review API returned 1 summary from `/Users/zz/Desktop/InnerNotes/reviews`.

Expected deployed behavior:

- AnyHost ready Postgres resource injects `DATABASE_URL`.
- Server starts in `postgres` storage mode.
- `/api/health` returns `{ "ok": true, "storage": "postgres" }`.
- Migration script imports local JSON entries and review summaries into Postgres.

AnyHost context after linking:

- Workspace: `wsp_39d9c19c-134f-4dd7-96fa-7ebd0c99c1f5` (`reizz25 Workspace`)
- Project: `prj_8925ad70-0979-4751-a4e9-19fdac5264ef` (`innernote`)
- Repository: `reizz25/innernote`
- Environments: `dev`, `prod`
- Permissions: `context:read`, `deploy:dev`, `logs:read`
- Managed resources: none
- Dev Postgres: `No ready Postgres resources found for dev.`

Dev deployment requested:

- Deployment: `dep_2232b266-90e1-4849-b72d-412d984fb186`
- Commit: `04256214dbc5de4373b044bf2f8b6e2915062850`
- Status observed: `succeeded`
- Summary: `deployed innernote dev to https://innernote-dev.anyhostcloud.com`
- Public URL: `https://innernote-dev.anyhostcloud.com`
- Logs URL: `https://console.aws.amazon.com/cloudwatch/home?region=us-west-2#logsV2:log-groups/log-group/$252Fanyhost$252Fproject-builder/log-events/76a88397-a1dc-4b62-939d-98e8174451a8`

## Verification

Automated tests passed:

```text
tests 76
pass 76
fail 0
```

Local smoke test passed:

```text
GET /api/health -> 200 {"ok":true,"storage":"file"}
GET /api/journals -> 200 entries=6 summaries=1
```

AnyHost deployment status observed:

```text
Project: innernote (prj_8925ad70-0979-4751-a4e9-19fdac5264ef)
Repository: reizz25/innernote
Env: dev
Deployment: dep_2232b266-90e1-4849-b72d-412d984fb186
Status: running
Commit: 0425621
Summary: runner claimed job
```

Final AnyHost status:

```text
Deployment: dep_2232b266-90e1-4849-b72d-412d984fb186
Status: succeeded
Commit: 0425621
URL: https://innernote-dev.anyhostcloud.com
```

Dev health check passed:

```text
GET https://innernote-dev.anyhostcloud.com/api/health
200 {"ok":true,"storage":"file"}
```

## Risks

- AnyHost dev environment has no ready Postgres resource, so database migration cannot run yet.
- Dev currently runs with `file` storage because `DATABASE_URL` is not injected yet. This is fine for smoke testing but not durable storage.
- Postgres migration has been unit-tested against the storage contract but has not run against a live AnyHost Postgres resource.
- Image assets still use local filesystem storage. Notes text and review summaries are database-ready; pasted image persistence should move to object storage before relying on it in production.

## Follow-Up

1. Create a dev Postgres resource in AnyHost Resources.
2. Rerun `anyhost context` and confirm the dev Postgres resource is ready.
3. Redeploy with `anyhost deploy -e dev` so `DATABASE_URL` is injected.
4. Verify `GET /api/health` returns `"storage":"postgres"`.
5. Run `npm run migrate:journals` with a dev database access profile or an equivalent authorized `DATABASE_URL`.
